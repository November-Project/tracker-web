{{#if multiTime}}
  <hr style="margin-top: 10px; margin-bottom: 10px;">

  <div class="center">
    <div class="btn-group" role="group">
      {{#each times as |time index|}}
        <button type="button" class="btn btn-default {{unless index 'active'}}" data-time={{time}} {{action 'changeTime' time}}>{{time}}</button>
      {{/each}}
    </div>
  </div>
{{/if}}

<hr style="margin-top: 10px; margin-bottom: 10px;">

{{#if userHasSubmitted}}
  <div class="people">
    {{result-item model=submittedResult index=null workout=workout editable=true}}
  </div>
{{else}}
  {{#link-to 'events.view.results.new' (query-params time=selectedTime) class="btn btn-primary btn-block btn-lg"}}Record a {{selectedTime}} Result{{/link-to}}
{{/if}}

<span style="display:block;margin-top:10px;">{{filteredResults.length}} Result(s) Submitted</span>

<hr style="margin-top: 10px; margin-bottom: 0px;">

<div class="people">
  {{#each filteredResults as |result index|}}
    {{result-item model=result index=index workout=workout}}
  {{/each}}
</div>
